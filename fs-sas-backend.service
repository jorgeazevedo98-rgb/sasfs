[Unit]
Description=Filesystem SAS Backend
After=network.target

[Service]
Type=simple
User=root
WorkingDirectory=/opt/filesystem-sas/backend
ExecStart=/usr/bin/npm run dev
Restart=always

[Install]
WantedBy=multi-user.target
