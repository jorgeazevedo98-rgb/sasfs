[Unit]
Description=Filesystem SAS Frontend
After=network.target

[Service]
Type=simple
User=root
WorkingDirectory=/opt/filesystem-sas/frontend
ExecStart=/usr/bin/npm run dev
Restart=always

[Install]
WantedBy=multi-user.target
